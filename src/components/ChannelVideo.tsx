import React from 'react'
import { Item } from '../Types/types'

interface IProps{
    videos:Item | undefined
}

const ChannelVideo = ({videos}:IProps) => {
  return (
    <div>ChannelVideo</div>
  )
}

export default ChannelVideo
